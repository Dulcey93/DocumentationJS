const mensaje = 'Hola Mundo!';
const terminaConMundo = mensaje.endsWith('Mundo!');
console.log(terminaConMundo); // salida: true

// En este ejemplo, el método String.endsWith() se utiliza para verificar si una cadena de texto termina con una subcadena dada. La variable mensaje contiene una cadena de texto que dice "Hola Mundo!". Luego, el método endsWith() se utiliza para verificar si la cadena de texto termina con la subcadena "Mundo!". El resultado de la verificación se almacena en la variable terminaConMundo, que es un valor booleano (true o false).